#pragma once

#define WIN32_LEAN_AND_MEAN             // Exclude rarely-used stuff from Windows headers
#define NOMINMAX

// #define _WIN32_WINNT 0x0601 // go down to Windows 7 -> would need older compiler too...

// Windows Header Files
#include <windows.h>
